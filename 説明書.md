# 説明書 — spec-divisor-finder-agent

この文書は、IT や AI に詳しくない方に向けて、このリポジトリ（プログラムと文書の保管場所）が
何をするものかを、専門用語をなるべく使わずに説明するものです。

## 一言でいうと

「答えが1つに決まらない問題」を解くプログラムを AI に書かせ、
その答えが正しいかどうかを、別のプログラムが自動で確かめる仕組みの見本です。

題材は算数の「約数」です。
たとえば 12 は、2 でも 3 でも 4 でも 6 でも割り切れます。
「12 を割り切る数を1つ挙げてください」という問題には、正解が複数あります。

正解が1つに決まらないと、
「あらかじめ用意した正解と同じかどうか」を見る単純な丸付けができません。
そこでこの仕組みでは、答えそのものを正解表と比べるのではなく、
「答えが満たすべき条件」を確かめる方法で丸付けをしています。

## 何の役に立つのか

AI に仕事を任せる場面では、AI が出した結果が正しいかどうかを
人間が毎回確かめていては、手間がかかりすぎます。
確認まで自動化できれば、安心して任せられる範囲が広がります。

ただし、世の中の仕事の多くは「正解が1つに決まらない」形をしています。
文章の要約にも、会議の日程案にも、正しい答えは複数あります。
このリポジトリは、そうした仕事を自動で採点するための基本的な考え方を、
最小の題材（約数）で動く形にして示すものです。

たとえるなら、採点者が模範解答を持たずに、
「答案が決められた条件を守っているか」だけを見て丸付けをする方式です。

## 正しさをどう確かめているか

採点プログラム（eval/oracle.py）は、答えを次の条件と照らし合わせます。

- 「割り切る数」を答えた場合。
  その数が 1 でも元の数自身でもなく、実際に割り切れることを、
  採点プログラムが自分で計算して確かめます。
- 「割り切る数はない」と答えた場合。
  本当にないかどうかを、採点プログラムが自分で割り算をやり直して確かめます。

これを 2 から 5000 までのすべての数について行い、
1つでも条件を破れば不合格、すべて満たせば合格とします。

さらに、採点プログラム自身が信頼できるかどうかも確認しています。
正しいお手本の答案には合格を、わざと間違えて作った3種類の答案には不合格を、
それぞれ正しく出せるかを試す機能（自己検証）を備えています。
採点者の目が確かかどうかを、先に試験しておくということです。

## フォルダとファイルの案内

- `README.md` … 全体の紹介（技術者向け）。
- `説明書.md` … この文書。
- `.claude/agents/` … AI への作業指示書が入っている場所。
- `eval/oracle.py` … 採点プログラム。
- `eval/corpus/` … 採点対象の答案の置き場所。
  お手本（`reference.py`）と、わざと間違えた答案（`broken_` で始まるファイル）が入っています。
- `design/design.md` … なぜこの設計にしたかの記録（技術者向け）。
- `.github/workflows/ci.yml` … push のたびに `--selftest` を自動実行（CI）。

## 用語の小辞典

- リポジトリ … プログラムや文書をひとまとめにした保管場所。
- エージェント … 指示を受けて、必要な手順を自分で進めて作業する AI プログラム。
- オラクル … 結果が正しいかどうかを判定する採点プログラムのこと。
- 約数 … ある整数を割り切る整数。12 の約数は 1, 2, 3, 4, 6, 12。
- 素数 … 1 と自分自身でしか割り切れない 2 以上の整数。2, 3, 5, 7 など。
